#!/usr/bin/env bash
set -e
if [[ ! -d "/data/home/poelzi/Projects/musicCommander" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/data/home/poelzi/Projects/musicCommander")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/data/home/poelzi/Projects/musicCommander" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/data/home/poelzi/Projects/musicCommander/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/data/home/poelzi/Projects/musicCommander/.envrc" "/data/home/poelzi/Projects/musicCommander/.direnv"/*.rc
